# EaseBiz AI

An MSME compliance assistant that helps small business owners in India quickly identify the licenses, government schemes, and export requirements relevant to their business — surfaced through a dashboard with real, live market-analysis output.

## Features

- **Compliance lookup** — surfaces relevant licenses, government schemes, and export requirements based on business inputs
- **Live AI analysis** — Market Analysis, Expert Advice, and Competition sections generated by a real LLM call (Groq), with a static fallback so the app still runs with zero API setup
- **Server-side PDF reports** — compliance report generation via `reportlab`
- **Verified static data** — license fees, timelines, and scheme links are hand-verified rather than LLM-generated, so users aren't shown hallucinated facts
- **Tailwind-based UI** — clean, styled interface with a consistent design system throughout

## Tech Stack

| Layer | Technology |
|---|---|
| Backend | Python, Flask, Jinja2 |
| AI | Groq (free-tier LLM API) |
| PDF generation | reportlab (server-side) |
| Styling | Tailwind CSS (CDN) + custom CSS |
| Icons | Lucide (CDN) |
| Animation | Lightweight CSS fade-in |

## Project Structure

```
easebiz-ai/
├── app.py                 # Flask routes
├── llm_service.py         # Groq LLM integration + static fallback
├── pdf_generator.py       # Server-side PDF report (reportlab)
├── requirements.txt
├── .env.example
├── templates/
│   ├── base.html          # navbar, footer, Tailwind config
│   ├── index.html         # landing page (Hero, Features, Licenses, Schemes, Dashboard form)
│   └── analysis.html      # tabbed AI analysis results page
└── static/
    ├── css/style.css      # glass/gradient-text/hero-gradient styles
    └── js/
        ├── main.js        # navbar scroll effect, mobile menu, form loading state
        └── analysis.js    # tab switching on the analysis page
```

## Setup & Running Locally

1. **Clone and open the project**
   ```bash
   git clone https://github.com/<your-username>/easebiz-ai.git
   cd easebiz-ai
   ```

2. **Create a virtual environment and install dependencies**
   ```bash
   python -m venv .venv
   source .venv/bin/activate      # on Windows: .venv\Scripts\activate
   pip install -r requirements.txt
   ```

3. **Set up your free LLM key (optional but recommended)**
   - Copy `.env.example` to `.env`
   - Get a free key (no credit card required) at [console.groq.com/keys](https://console.groq.com/keys) and set `GROQ_API_KEY=...` in `.env`
   - Without a key, the app still runs fully — it falls back to well-written static content instead of live-generated analysis

4. **Run the app**
   ```bash
   python app.py
   ```

5. Open **http://127.0.0.1:5000** in your browser.

## Deployment

This is a standard Flask app and runs anywhere Python does (Render, Railway, PythonAnywhere, Fly.io, or a VPS). For production, run it behind gunicorn instead of the Flask dev server:

```bash
pip install gunicorn
gunicorn -w 2 -b 0.0.0.0:8000 app:app
```

Set `GROQ_API_KEY` and `FLASK_SECRET_KEY` as real environment variables on your host — don't rely on the `.env` file in production. A `.gitignore` is included so `.venv/` and `.env` never get committed.

## Notes

- `llm_service.py` and `pdf_generator.py` are framework-agnostic (pure Python, no Flask imports), so they can be reused as-is in a FastAPI version — only `app.py` and the templates' `url_for(...)` calls are Flask-specific.
- Built as part of a 6th-semester minor project (MSME compliance SaaS concept).
